import { CenteredLayout } from '@/layout'
import { LoginContainer } from '@/pods/login'

export const LoginScene = () => {
  return (
    <CenteredLayout>
      <LoginContainer />
    </CenteredLayout>
  )
}
